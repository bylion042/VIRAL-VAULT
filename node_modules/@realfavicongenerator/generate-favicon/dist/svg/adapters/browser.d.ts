/// <reference types="node" />
import { ImageAdapter } from ".";
export declare const svgToImageAndMeta: (dataUrl: string, size: number) => Promise<Buffer>;
export declare const getBrowserImageAdapter: () => ImageAdapter;
